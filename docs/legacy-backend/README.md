# Legacy Backend (Reference Only)

This was the original Express + PostgreSQL server before the API was migrated to Supabase Edge Functions (Deno).

Not used in production. Kept here as a reference for the original architecture and SQL schema.

The live API now lives in `supabase/functions/`.
